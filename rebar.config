%%-*- mode: erlang -*-

{deps, [
    {parselib, ".*",
        {git, "https://github.com/kostyushkin/parselib.git", "v2.1.0"}
    }
]}.

{eunit_opts, [
    verbose, 
    {report, {eunit_surefire, [{dir, "."}]}}
]}.

{clean_files, ["ebin", "deps"]}.
